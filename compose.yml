version: '3.8'

services:
  website:
    image: nginx:alpine                     
    container_name: javakotlin
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./src:/usr/share/nginx/html:ro
      - ./nginx-conf:/etc/nginx/conf.d:ro
    restart: unless-stopped